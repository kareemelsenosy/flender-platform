[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["mkdir -p uploads output data credentials"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2"
